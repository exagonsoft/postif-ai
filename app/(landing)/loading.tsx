"use client";
import React from "react";

const Loading = () => {
  return <div className="fixed z-[100] top-0 left-0 w-full h-screen flex justify-center items-center">LOADING...</div>;
};

export default Loading;
